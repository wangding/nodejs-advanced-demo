load data infile 'areas.data' replace into table areas;
load data infile 'tags.data' replace into table tags;
load data infile 'authors.data' replace into table authors;
load data infile 'books.data' replace into table books;
load data infile 'author-books.data' replace into table author_books;
